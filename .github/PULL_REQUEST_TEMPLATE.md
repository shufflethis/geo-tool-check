## What does this change?

<!-- One or two sentences. Link the issue if one exists. -->

## Checklist

- [ ] `npm run check` passes (format, lint, typecheck, tests)
- [ ] `npm run verify:package` passes
- [ ] No scoring weights, thresholds, categories, or JSON field names changed
- [ ] CLI flags, exit codes, and MCP tool names remain compatible
- [ ] The package still has zero runtime dependencies and never fetches geo-tool.com
- [ ] Docs updated where behavior or usage changed
